package usecase;

/**
 * Marker interface: no methods.
 * Used to tag devices that are critical (e.g. security cameras).
 */
public interface CriticalDevice {
    // no methods – this is a marker / flag interface
}

